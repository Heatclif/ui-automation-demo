# Selenium has no built-in per-project baseURL concept (unlike Playwright's
# per-project `use.baseURL`), so this file is the substitute: one base URL
# per site, read by each site's BaseTest.
saucedemo.baseUrl=https://www.saucedemo.com
wdu.baseUrl=https://webdriveruniversity.com
